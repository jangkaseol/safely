---
alwaysApply: true
description: 카카오 맵 통합 및 충돌 방지 가이드
---

# 카카오 맵 라이브러리 통합 가이드

## ⚠️ 중요: 라이브러리 충돌 방지

같은 프로젝트에서 **react-kakao-maps-sdk**와 **순수 카카오 맵 SDK**를 함께 사용할 때 발생할 수 있는 충돌을 방지하는 방법입니다.

## 🎯 해결된 문제

### 원인 1: services 라이브러리 누락

```javascript
// ❌ 문제: services 라이브러리 없음
//dapi.kakao.com/v2/maps/sdk.js?appkey=...&autoload=false

// ✅ 해결: libraries=services 추가
//dapi.kakao.com/v2/maps/sdk.js?appkey=...&libraries=services&autoload=false
```

### 원인 2: autoload 설정 충돌

```javascript
// ❌ 문제: react-kakao-maps-sdk와 충돌
autoload = true;

// ✅ 해결: react-kakao-maps-sdk가 초기화 관리
autoload = false;
```

### 원인 3: 초기화 순서 문제

```javascript
// ❌ 문제: 순수 SDK에서 직접 스크립트 로드
<Script src="..." onLoad={handleScriptLoad} />;

// ✅ 해결: react-kakao-maps-sdk 초기화 후 services 사용
useEffect(() => {
  window.kakao.maps.load(() => setIsReady(true));
}, []);
```

## 📋 필수 설정

### 1. layout.tsx 설정

```typescript
// [layout.tsx](mdc:app/layout.tsx)에서 반드시 다음 설정 유지:
<Script
  src={`//dapi.kakao.com/v2/maps/sdk.js?appkey=${KAKAO_MAP_API_KEY}&libraries=services&autoload=false`}
  strategy="beforeInteractive"
/>
```

### 2. KakaoMap 컴포넌트

```typescript
// [kakao-map.tsx](mdc:components/kakao-map.tsx)에서 수동 초기화:
useEffect(() => {
  if (window.kakao && window.kakao.maps) {
    window.kakao.maps.load(() => {
      setIsKakaoLoaded(true);
    });
  }
}, []);
```

### 3. AddressSearch 컴포넌트

```typescript
// [AddressSearch.tsx](mdc:components/register/AddressSearch.tsx)에서 초기화 대기:
useEffect(() => {
  const checkKakaoReady = () => {
    return window.kakao?.maps?.services;
  };
  // KakaoMap 초기화 완료까지 주기적 체크
}, []);
```

## 🚨 절대 하지 말아야 할 것들

1. **autoload=true로 변경하지 말 것**

   - `TypeError: kakao.maps.LatLng is not a constructor` 오류 발생

2. **libraries=services 누락하지 말 것**

   - `window.kakao.maps.services is undefined` 오류 발생

3. **여러 곳에서 Script 태그 중복 로드하지 말 것**

   - 스크립트 충돌 및 초기화 오류 발생

4. **순수 SDK로 직접 초기화하지 말 것**
   - react-kakao-maps-sdk와 충돌 발생

## ✅ 올바른 사용 패턴

### react-kakao-maps-sdk 사용

```typescript
import { Map, MapMarker } from "react-kakao-maps-sdk";

// KakaoMap 컴포넌트에서만 사용
<Map center={center} onCreate={setMap}>
  <MapMarker position={position} />
</Map>;
```

### 순수 SDK 사용 (Geocoder 등)

```typescript
// AddressSearch 등에서 services 사용
const geocoder = new window.kakao.maps.services.Geocoder();
geocoder.addressSearch(address, callback);
```

## 🔍 디버깅 팁

초기화 상태 확인:

```javascript
console.log({
  kakao: !!window.kakao,
  maps: !!window.kakao?.maps,
  services: !!window.kakao?.maps?.services,
});
```

모든 값이 `true`여야 정상 동작합니다.
